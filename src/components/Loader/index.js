import React from "react";

const Loader = () => {
  return (
    <div className="loader">
      {/* You can customize the loader as per your needs */}
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
